#include <iostream>
#include <string>

using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        char c;
        cin >> c >> c;
        int a, b;
        cin >> a >> c >> b;
        cout << b - a << endl;
    }
	//code
	return 0;
}
